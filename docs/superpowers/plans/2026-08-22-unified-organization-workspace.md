# 统一组织架构工作台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将成员、团队、邀请、访问角色和员工房源分工整合为一个现代化的双栏“组织架构”工作台，并保留旧路由兼容。

**Architecture:** 后端新增只面向工作台的轻量导航、成员目录和全局搜索读取接口，并为团队成员关系提供原子加入/移除操作；现有角色、邀请和房源分工领域接口继续复用。管理端新增独立 `space/organization` 页面目录，以 URL 查询参数保存一级标签、组织树节点、详情标签和动作状态；旧页面源码暂时保留，旧路由统一重定向到新工作台。

**Tech Stack:** Django 5、django-ninja、PostgreSQL、pytest；React 19、Umi Max 4、Ant Design 6、ProComponents 3、TanStack Query、antd-style、Vitest、Testing Library。

---

## 执行约束

- 设计依据：`docs/superpowers/specs/2026-08-22-unified-organization-workspace-design.md`。
- 本仓库默认不执行 Git 操作；每个任务以测试通过作为检查点，只有用户另行授权时才提交。
- 前端任何 Node 命令前先在 `frontend_admin/` 执行 `nvm use 22`。
- 不手改 `frontend_admin/src/services/openapi/`；后端 schema 完成后统一运行 `npm run openapi`。
- 不修改多语言文件、`package.json` 或 `package-lock.json`。新菜单使用路由 `locale: false` 和中文名称。
- 不物理删除原成员、团队、邀请、权限或员工分工页面；只改路由入口。
- 浏览器验收截图写入 `.codex/audits/unified-organization-workspace/`，不写项目根目录。

## 文件结构

### 后端

- Create: `apps/organizations/workspace_api.py` — 工作台导航、成员目录、成员详情和全局搜索 API。
- Create: `apps/organizations/workspace_schemas.py` — 工作台专用轻量输出 schema。
- Create: `apps/teams/services.py` — 当前用户可见团队 queryset，供团队 API 和工作台 API 复用。
- Modify: `apps/teams/api.py` — 复用可见团队查询；增加原子成员加入/移除并清理团队角色绑定。
- Modify: `apps/teams/schemas.py` — 团队摘要与成员变更结果 schema。
- Modify: `apps/house/api.py` — 增加单成员分工读取和按团队过滤分工列表。
- Modify: `apps/organizations/schemas.py` — 邀请输出增加 `is_expired`。
- Modify: `config/api.py` — 挂载 `/api/organization-workspace/`。
- Create: `tests/organizations/test_workspace_api.py` — 工作台读取 API 测试。
- Modify: `tests/organizations/test_api.py` — 邀请过期状态测试。
- Modify: `tests/teams/test_api.py` — 原子团队成员变更和角色清理测试。
- Modify: `tests/house/test_responsibilities.py` — 单成员读取与团队过滤测试。

### 管理端

- Modify: `frontend_admin/src/utils/adminRouting.ts` — 新增规范路由常量。
- Modify: `frontend_admin/config/routes.ts` — 注册新页面、收敛菜单、配置旧路由重定向。
- Modify: `frontend_admin/src/routes.test.ts` — 路由结构和兼容跳转测试。
- Create: `frontend_admin/src/pages/space/organization/model.ts` — URL 状态解析、校验和构建。
- Create: `frontend_admin/src/pages/space/organization/model.test.ts` — URL 状态测试。
- Create: `frontend_admin/src/pages/space/organization/queryKeys.ts` — 工作台查询键。
- Create: `frontend_admin/src/pages/space/organization/index.tsx` — 页面壳、一级标签、主要动作和移动端组织树 Drawer。
- Create: `frontend_admin/src/pages/space/organization/styles.ts` — 只使用 Ant Design token 的双栏和选中态样式。
- Create: `frontend_admin/src/pages/space/organization/components/OrganizationTreePanel.tsx` — 组织树、团队按需成员加载和全局搜索结果。
- Create: `frontend_admin/src/pages/space/organization/components/OrganizationOverviewPanel.tsx` — 组织治理摘要和异常入口。
- Create: `frontend_admin/src/pages/space/organization/components/AllMembersPanel.tsx` — 默认全部成员目录与添加已有用户。
- Create: `frontend_admin/src/pages/space/organization/components/MemberWorkspacePanel.tsx` — 成员资料、所属团队、访问角色、房源分工。
- Create: `frontend_admin/src/pages/space/organization/components/TeamWorkspacePanel.tsx` — 团队资料、成员、团队角色、分工概览。
- Create: `frontend_admin/src/pages/space/organization/components/RolePermissionPanel.tsx` — 组织级和团队级角色定义。
- Create: `frontend_admin/src/pages/space/organization/components/InvitationWorkspacePanel.tsx` — 邀请列表、创建、详情、重发和取消。
- Create: `frontend_admin/src/pages/space/organization/components/ResponsibilityEditor.tsx` — 三级房源分工编辑和已负责房源预览。
- Create: `frontend_admin/src/pages/space/organization/hooks/useUnsavedWorkspaceGuard.ts` — 节点、标签和路由切换保护。
- Create: matching `*.test.tsx` files beside the page/components/hooks above.
- Modify: `frontend_admin/src/components/AppIcon/index.tsx` — 邀请对象及状态图标。
- Modify: `frontend_admin/src/components/AppStatus/registry.ts` — 邀请状态语义。
- Modify: matching AppIcon/AppStatus tests.
- Generated by `npm run openapi`: `frontend_admin/src/services/openapi/organizationWorkspace.ts`、`teams.ts`、`propertyRentalManagement.ts`、`organizationInvites.ts`、`typings.d.ts`、`index.ts`。

## Task 1：对齐现有 OpenSpec 提案

**Files:**

- Modify: `openspec/changes/revamp-space-member-management/proposal.md`

- [ ] **Step 1: 先验证旧提案确实冲突**

Run:

```bash
rg -n "独立的“员工分工”|不新增或修改后端 API|不调整空间管理导航结构|成员详情集中到抽屉" openspec/changes/revamp-space-member-management/proposal.md
```

Expected: 命中旧的独立成员页、无后端 API、导航不调整等描述。

- [ ] **Step 2: 将提案改为统一工作台范围**

将 `What Changes` 和 `Impact` 改为明确包含以下内容：

```markdown
- 将成员、团队、邀请、权限管理和员工分工合并为“组织架构”工作台。
- 页面顶部使用“组织成员 / 角色权限”一级标签，左侧始终保留统一组织树。
- 成员详情包含成员资料、所属团队、访问角色和房源分工；团队详情包含团队资料、团队成员、团队角色和成员分工概览。
- 邀请功能原样整合到“待加入”，接受邀请后进入未分组成员。
- 新增轻量组织导航、工作台成员目录/搜索、团队成员原子加入/移除 API，不修改数据库模型。
- 原成员、邀请、团队、员工分工和权限管理路由保留兼容重定向。

## Impact

- 影响 `apps/organizations/`、`apps/teams/`、`apps/house/` 的工作台支撑 API 及测试。
- 主要新增 `frontend_admin/src/pages/space/organization/`，复用现有角色、邀请和房源分工服务。
- 更新管理端空间管理路由；原页面源码暂不物理删除。
- 生成 OpenAPI 客户端，但不手改生成文件，不修改依赖和多语言文件。
```

- [ ] **Step 3: 验证冲突文字已消失**

Run:

```bash
rg -n "不新增或修改后端 API|不调整空间管理导航结构|成员详情集中到抽屉" openspec/changes/revamp-space-member-management/proposal.md
```

Expected: 无输出。

## Task 2：新增轻量组织工作台读取 API

**Files:**

- Create: `apps/teams/services.py`
- Create: `apps/organizations/workspace_schemas.py`
- Create: `apps/organizations/workspace_api.py`
- Modify: `apps/teams/api.py`
- Modify: `config/api.py`
- Create: `tests/organizations/test_workspace_api.py`

- [ ] **Step 1: 编写导航、成员目录和搜索失败测试**

在新测试文件中复用 `OrganizationAPITestBase` 的登录方式，至少添加以下测试：

```python
class TestOrganizationWorkspaceAPI(OrganizationAPITestBase):
    def test_navigation_returns_counts_and_lightweight_teams(self):
        team = baker.make("teams.Team", organization=self.org, name="租赁运营组")
        team.members.add(self.user)
        OrganizationInvite.objects.create(
            organization=self.org,
            sender=self.user,
            invitee_email="guest@example.com",
        )
        self._login()

        response = self.client.get("/api/organization-workspace/navigation/")

        self.assertEqual(response.status_code, 200)
        payload = api_data(response)
        self.assertEqual(payload["member_count"], 1)
        self.assertEqual(payload["team_count"], 1)
        self.assertEqual(payload["teams"][0], {"id": team.pk, "name": "租赁运营组", "member_count": 1})
        self.assertNotIn("member_details", payload["teams"][0])

    def test_member_directory_filters_team_and_ungrouped_members(self):
        grouped_user = User.objects.create_user(username="grouped", password="secret")  # noqa: S106
        ungrouped_user = User.objects.create_user(username="ungrouped", password="secret")  # noqa: S106
        grouped = baker.make("organizations.OrganizationMember", organization=self.org, user=grouped_user)
        ungrouped = baker.make("organizations.OrganizationMember", organization=self.org, user=ungrouped_user)
        team = baker.make("teams.Team", organization=self.org, name="资产组")
        team.members.add(grouped_user)
        self._login()

        team_response = self.client.get("/api/organization-workspace/members/", {"page": 1, "page_size": 20, "team_id": team.pk})
        ungrouped_response = self.client.get("/api/organization-workspace/members/", {"page": 1, "page_size": 20, "ungrouped": "true"})

        self.assertEqual([item["member_id"] for item in api_data(team_response)["items"]], [grouped.pk])
        self.assertEqual([item["member_id"] for item in api_data(ungrouped_response)["items"]], [ungrouped.pk])

    def test_search_deduplicates_member_and_returns_team_tags(self):
        employee = User.objects.create_user(username="alice", first_name="Alice", password="secret")  # noqa: S106
        member = baker.make("organizations.OrganizationMember", organization=self.org, user=employee)
        first = baker.make("teams.Team", organization=self.org, name="运营一组")
        second = baker.make("teams.Team", organization=self.org, name="运营二组")
        first.members.add(employee)
        second.members.add(employee)
        self._login()

        response = self.client.get("/api/organization-workspace/search/", {"keyword": "Alice"})

        self.assertEqual(response.status_code, 200)
        members = api_data(response)["members"]
        self.assertEqual([item["member_id"] for item in members], [member.pk])
        self.assertEqual({item["name"] for item in members[0]["teams"]}, {"运营一组", "运营二组"})
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```bash
docker compose exec web pytest tests/organizations/test_workspace_api.py -q
```

Expected: FAIL，原因是路由或 schema 尚不存在。

- [ ] **Step 3: 提取可见团队查询**

在 `apps/teams/services.py` 定义唯一入口，保持现有团队可见性规则：

```python
from apps.access.constants import TeamPermission
from apps.access.services import has_permission
from apps.teams.models import Team


def visible_teams_for_request(request, organization):
    qs = Team.objects.filter(organization=organization)
    if has_permission(request.user, organization, TeamPermission.VIEW):
        return qs.distinct()
    return qs.filter(
        teamgroupbinding__user=request.user,
        teamgroupbinding__group__access_role__is_active=True,
        teamgroupbinding__group__permissions__content_type__app_label="teams",
        teamgroupbinding__group__permissions__codename="team_view",
    ).distinct()
```

让 `apps/teams/api.py:_team_qs()` 调用此函数，并继续 `prefetch_related("members").order_by("name")`，确保旧团队接口行为不变。

- [ ] **Step 4: 定义工作台专用 schema**

`workspace_schemas.py` 使用以下稳定字段：

```python
from datetime import datetime

from ninja import Schema

from apps.organizations.schemas import OrgUserOut


class WorkspaceOrganizationOut(Schema):
    id: int
    name: str
    slug: str


class WorkspaceTeamSummaryOut(Schema):
    id: int
    name: str
    member_count: int


class WorkspaceMemberOut(Schema):
    member_id: int
    user: OrgUserOut
    is_owner: bool
    teams: list[WorkspaceTeamSummaryOut]
    has_responsibility: bool
    created_at: datetime
    updated_at: datetime

    @staticmethod
    def resolve_member_id(obj):
        return obj.pk

    @staticmethod
    def resolve_user(obj):
        return obj.user

    @staticmethod
    def resolve_teams(obj):
        return getattr(obj.user, "workspace_teams", [])


class OrganizationNavigationOut(Schema):
    organization: WorkspaceOrganizationOut
    member_count: int
    owner_count: int
    team_count: int
    ungrouped_member_count: int
    pending_invite_count: int | None
    unassigned_responsibility_count: int
    teams: list[WorkspaceTeamSummaryOut]


class OrganizationSearchOut(Schema):
    teams: list[WorkspaceTeamSummaryOut]
    members: list[WorkspaceMemberOut]
```

- [ ] **Step 5: 实现工作台 router**

`workspace_api.py` 必须包含以下接口与约束：

```python
router = Router(tags=["租户/组织架构"])


@router.get("/navigation/", response=OrganizationNavigationOut, summary="获取组织架构导航摘要")
def get_navigation(request):
    org = require_org_permission(request, OrganizationPermission.MEMBER_VIEW)
    members = OrganizationMember.objects.filter(organization=org)
    teams = visible_teams_for_request(request, org).annotate(member_count=Count("members", distinct=True)).order_by("name", "pk")
    ungrouped_count = members.annotate(
        organization_team_count=Count("user__teams", filter=Q(user__teams__organization=org), distinct=True)
    ).filter(organization_team_count=0).count()
    pending_invites = None
    if has_permission(request.user, org, OrganizationPermission.INVITE_MANAGE):
        pending_invites = OrganizationInvite.objects.filter(
            organization=org,
            created_at__gte=timezone.now() - timedelta(days=OrganizationInvite.expired_in_days),
        ).count()
    return {
        "organization": org,
        "member_count": members.count(),
        "owner_count": members.filter(is_owner=True).count(),
        "team_count": teams.count(),
        "ungrouped_member_count": ungrouped_count,
        "pending_invite_count": pending_invites,
        "unassigned_responsibility_count": members.filter(property_responsibilities__isnull=True).count(),
        "teams": list(teams),
    }


@router.get("/members/", response=list[WorkspaceMemberOut], summary="获取组织架构成员目录")
@paginate(LegacyPagination)
def list_workspace_members(
    request,
    keyword: str | None = Query(None),
    team_id: int | None = Query(None),
    ungrouped: bool = Query(False),
):
    org = require_org_permission(request, OrganizationPermission.MEMBER_VIEW)
    qs = workspace_members_queryset(request, org)
    if keyword:
        qs = qs.filter(
            Q(user__first_name__icontains=keyword)
            | Q(user__last_name__icontains=keyword)
            | Q(user__username__icontains=keyword)
            | Q(user__email__icontains=keyword)
        )
    if team_id is not None:
        team = get_object_or_404(visible_teams_for_request(request, org), pk=team_id)
        qs = qs.filter(user__teams=team)
    if ungrouped:
        qs = qs.filter(organization_team_count=0)
    return qs.distinct()


@router.get("/members/{member_id}/", response=WorkspaceMemberOut, summary="获取组织架构成员详情")
def get_workspace_member(request, member_id: int):
    org = require_org_permission(request, OrganizationPermission.MEMBER_VIEW)
    return get_object_or_404(workspace_members_queryset(request, org), pk=member_id)


@router.get("/search/", response=OrganizationSearchOut, summary="搜索组织架构")
def search_workspace(request, keyword: str = Query("")):
    org = require_org_permission(request, OrganizationPermission.MEMBER_VIEW)
    normalized = keyword.strip()
    if not normalized:
        return {"teams": [], "members": []}
    teams = visible_teams_for_request(request, org).filter(name__icontains=normalized).annotate(member_count=Count("members", distinct=True)).order_by("name", "pk")[:10]
    members = workspace_members_queryset(request, org).filter(
        Q(user__first_name__icontains=normalized)
        | Q(user__last_name__icontains=normalized)
        | Q(user__username__icontains=normalized)
        | Q(user__email__icontains=normalized)
    ).distinct()[:20]
    return {"teams": list(teams), "members": list(members)}
```

`workspace_members_queryset()` 使用以下查询结构，团队标签、未分组判断和房源分工状态均在数据库查询中完成：

```python
def workspace_members_queryset(request, org):
    visible_teams = visible_teams_for_request(request, org).annotate(
        member_count=Count("members", distinct=True)
    ).order_by("name", "pk")
    return (
        OrganizationMember.objects.filter(organization=org)
        .select_related("user")
        .annotate(
            organization_team_count=Count(
                "user__teams",
                filter=Q(user__teams__organization=org),
                distinct=True,
            ),
            has_responsibility=Exists(
                PropertyResponsibility.objects.filter(member_id=OuterRef("pk"))
            ),
        )
        .prefetch_related(
            Prefetch(
                "user__teams",
                queryset=visible_teams,
                to_attr="workspace_teams",
            )
        )
        .order_by("user__first_name", "user__last_name", "user__username", "pk")
    )
```

- [ ] **Step 6: 挂载 router 并运行测试**

在 `config/api.py` 导入 `workspace_api.router`，挂载：

```python
api.add_router("/organization-workspace/", organization_workspace_router)
```

Run:

```bash
docker compose exec web pytest tests/organizations/test_workspace_api.py tests/teams/test_api.py::TestTeamAPI::test_list -q
```

Expected: PASS；旧团队列表测试仍通过。

## Task 3：实现团队成员原子加入、移除和角色清理

**Files:**

- Modify: `apps/teams/schemas.py`
- Modify: `apps/teams/api.py`
- Modify: `tests/teams/test_api.py`

- [ ] **Step 1: 编写失败测试**

在 `TestTeamAPI` 中添加：

```python
def test_add_team_member_is_idempotent(self):
    employee = User.objects.create_user(username="employee", password="secret")  # noqa: S106
    baker.make("organizations.OrganizationMember", organization=self.org, user=employee)
    team = baker.make("teams.Team", organization=self.org)
    self._login()

    first = self.client.post(f"/api/teams/{team.pk}/members/{employee.pk}/")
    second = self.client.post(f"/api/teams/{team.pk}/members/{employee.pk}/")

    self.assertEqual(api_data(first)["changed"], True)
    self.assertEqual(api_data(second)["changed"], False)
    self.assertEqual(team.members.filter(pk=employee.pk).count(), 1)

def test_remove_team_member_cleans_team_role_bindings(self):
    employee = User.objects.create_user(username="employee", password="secret")  # noqa: S106
    baker.make("organizations.OrganizationMember", organization=self.org, user=employee)
    team = baker.make("teams.Team", organization=self.org)
    team.members.add(employee)
    group = make_access_group("employee_team_role", AccessScope.TEAM, [("teams", "team_view")])
    binding = bind_team_role(team, employee, group)
    self._login()

    response = self.client.delete(f"/api/teams/{team.pk}/members/{employee.pk}/")

    self.assertEqual(response.status_code, 200)
    self.assertEqual(api_data(response)["changed"], True)
    self.assertFalse(team.members.filter(pk=employee.pk).exists())
    self.assertFalse(type(binding).objects.filter(pk=binding.pk).exists())

def test_add_team_member_rejects_user_from_other_organization(self):
    outsider = User.objects.create_user(username="outsider", password="secret")  # noqa: S106
    baker.make("organizations.OrganizationMember", user=outsider)
    team = baker.make("teams.Team", organization=self.org)
    self._login()

    response = self.client.post(f"/api/teams/{team.pk}/members/{outsider.pk}/")

    self.assertEqual(response.status_code, 404)
    self.assertFalse(team.members.filter(pk=outsider.pk).exists())
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```bash
docker compose exec web pytest tests/teams/test_api.py -q
```

Expected: 新测试 FAIL，现有测试仍可运行。

- [ ] **Step 3: 定义返回 schema 和共享移除函数**

在 `apps/teams/schemas.py` 增加：

```python
class TeamMemberMutationOut(Schema):
    team_id: int
    user_id: int
    changed: bool
```

在 `apps/teams/api.py` 增加 `_remove_members_and_bindings(team, user_ids)`，先 `team.members.remove(*user_ids)`，再执行：

```python
TeamGroupBinding.objects.filter(team=team, user_id__in=user_ids).delete()
```

现有 `patch_team()` 在整体替换前记录旧成员 ID，`set()` 后对差集调用该清理逻辑，确保旧接口也不会留下失效团队角色。

- [ ] **Step 4: 实现原子端点**

```python
@router.post("/{team_id}/members/{user_id}/", response=TeamMemberMutationOut, summary="加入团队成员")
def add_team_member(request, team_id: int, user_id: int):
    permitted_team = require_team_permission(request, team_id, TeamPermission.MEMBER_MANAGE)
    get_object_or_404(OrganizationMember, organization=permitted_team.organization, user_id=user_id)
    with transaction.atomic():
        team = Team.objects.select_for_update().get(pk=permitted_team.pk)
        changed = not team.members.filter(pk=user_id).exists()
        if changed:
            team.members.add(user_id)
    return {"team_id": team.pk, "user_id": user_id, "changed": changed}


@router.delete("/{team_id}/members/{user_id}/", response=TeamMemberMutationOut, summary="移除团队成员")
def remove_team_member(request, team_id: int, user_id: int):
    permitted_team = require_team_permission(request, team_id, TeamPermission.MEMBER_MANAGE)
    with transaction.atomic():
        team = Team.objects.select_for_update().get(pk=permitted_team.pk)
        changed = team.members.filter(pk=user_id).exists()
        if changed:
            _remove_members_and_bindings(team, [user_id])
    return {"team_id": team.pk, "user_id": user_id, "changed": changed}
```

- [ ] **Step 5: 运行团队与权限回归测试**

Run:

```bash
docker compose exec web pytest tests/teams/test_api.py tests/access/test_api.py -q
```

Expected: PASS。







## Task 4：补齐单成员房源分工读取、团队过滤和邀请过期状态

**Files:**

- Modify: `apps/house/api.py`
- Modify: `apps/organizations/schemas.py`
- Modify: `tests/house/test_responsibilities.py`
- Modify: `tests/organizations/test_api.py`

- [ ] **Step 1: 编写房源分工失败测试**

在 `PropertyResponsibilityApiTestCase` 中添加：

```python
def test_get_single_member_responsibility(self):
    PropertyResponsibility.objects.create(organization=self.org, member=self.member, estate=self.estate)

    response = self.client.get(f"/api/house/staff-responsibilities/{self.member.pk}/")

    self.assertEqual(response.status_code, 200)
    payload = api_data(response)
    self.assertEqual(payload["member_id"], self.member.pk)
    self.assertEqual([item["id"] for item in payload["estates"]], [self.estate.pk])

def test_list_responsibilities_filters_by_team(self):
    team = baker.make("teams.Team", organization=self.org)
    team.members.add(self.employee)

    response = self.client.get(
        "/api/house/staff-responsibilities/",
        {"page": 1, "page_size": 20, "team_id": team.pk},
    )

    self.assertEqual([item["member_id"] for item in api_data(response)["items"]], [self.member.pk])
```

- [ ] **Step 2: 增加 GET 与 `team_id` 过滤**

将列表函数签名改为：

```python
def list_staff_responsibilities(
    request,
    keyword: str | None = Query(None),
    team_id: int | None = Query(None),
):
```

当 `team_id` 存在时，用当前组织和 `TeamPermission.VIEW` 校验团队，再执行 `qs.filter(user__teams=team)`。在 PUT 路由旁增加：

```python
@router.get("/staff-responsibilities/{member_id}/", response=PropertyResponsibilityMemberOut, summary="获取员工房源职责")
def get_staff_responsibility(request, member_id: int):
    org = require_org_permission(request, OrganizationPermission.MEMBER_VIEW)
    return get_object_or_404(_property_responsibility_members_qs(org), pk=member_id)
```

- [ ] **Step 3: 邀请输出增加后端过期判断**

在 `InviteOut` 增加：

```python
is_expired: bool

@staticmethod
def resolve_is_expired(obj) -> bool:
    return obj.is_expired
```

在 `tests/organizations/test_api.py` 创建一条 `created_at` 超过 7 天的邀请，断言列表输出 `is_expired is True`；新邀请断言为 `False`。

- [ ] **Step 4: 运行相关后端测试**

Run:

```bash
docker compose exec web pytest tests/house/test_responsibilities.py tests/organizations/test_api.py -q
```

Expected: PASS。

## Task 5：重新生成 OpenAPI 客户端

**Files:**

- Generated: `frontend_admin/src/services/openapi/organizationWorkspace.ts`
- Generated/Modify: `frontend_admin/src/services/openapi/teams.ts`
- Generated/Modify: `frontend_admin/src/services/openapi/propertyRentalManagement.ts`
- Generated/Modify: `frontend_admin/src/services/openapi/organizationInvites.ts`
- Generated/Modify: `frontend_admin/src/services/openapi/typings.d.ts`
- Generated/Modify: `frontend_admin/src/services/openapi/index.ts`

- [ ] **Step 1: 确认后端 schema 可访问**

Run:

```bash
curl -fsS -o /dev/null http://localhost:18000/api/openapi.json
```

Expected: exit code 0。若服务未启动，使用项目现有开发启动方式启动后再继续；不要改用外部 schema。

- [ ] **Step 2: 使用 Node 22 生成客户端**

Run:

```bash
cd frontend_admin
nvm use 22
npm run openapi
```

Expected: 生成 `organizationWorkspace.ts`，并在相关服务中出现以下函数：

```text
appsOrganizationsWorkspaceApiGetNavigation
appsOrganizationsWorkspaceApiListWorkspaceMembers
appsOrganizationsWorkspaceApiGetWorkspaceMember
appsOrganizationsWorkspaceApiSearchWorkspace
appsTeamsApiAddTeamMember
appsTeamsApiRemoveTeamMember
appsHouseApiGetStaffResponsibility
```

这些名称由后端函数 `get_navigation`、`list_workspace_members`、`get_workspace_member`、`search_workspace`、`add_team_member`、`remove_team_member` 和 `get_staff_responsibility` 稳定生成；若名称不一致，应先检查后端函数或路由模块命名，不手工编辑生成文件。

- [ ] **Step 3: 检查生成类型**

Run:

```bash
rg -n "OrganizationNavigationOut|WorkspaceMemberOut|TeamMemberMutationOut|is_expired|team_id" src/services/openapi
```

Expected: 所有新字段和类型均可找到。

## Task 6：建立规范路由、兼容重定向和 URL 状态模型

**Files:**

- Modify: `frontend_admin/src/utils/adminRouting.ts`
- Modify: `frontend_admin/config/routes.ts`
- Modify: `frontend_admin/src/routes.test.ts`
- Create: `frontend_admin/src/pages/space/organization/model.ts`
- Create: `frontend_admin/src/pages/space/organization/model.test.ts`

- [ ] **Step 1: 先写路由结构失败测试**

将 `src/routes.test.ts` 的空间管理断言改为：

```tsx
expect(tenantGroup?.routes?.map((route) => route.path)).toEqual([
  SPACE_PATHS.root,
  SPACE_PATHS.organization,
  SPACE_PATHS.members,
  SPACE_PATHS.invitations,
  SPACE_PATHS.teams,
  SPACE_PATHS.responsibilities,
  SPACE_PATHS.access,
  SPACE_PATHS.profile,
  SPACE_PATHS.subscriptionOrders,
  SPACE_PATHS.subscription,
  SPACE_PATHS.settings,
  SPACE_PATHS.notificationDispatches,
]);

const organizationRoute = tenantGroup?.routes?.find(
  (route) => route.path === SPACE_PATHS.organization,
);
expect(organizationRoute).toMatchObject({
  name: '组织架构',
  locale: false,
  component: './space/organization',
});
expect(
  tenantGroup?.routes?.filter((route) =>
    [
      SPACE_PATHS.members,
      SPACE_PATHS.invitations,
      SPACE_PATHS.teams,
      SPACE_PATHS.responsibilities,
      SPACE_PATHS.access,
    ].includes(route.path as never),
  ).every((route) => route.hideInMenu),
).toBe(true);
```

- [ ] **Step 2: 为 URL 模型写失败测试**

`model.test.ts` 覆盖默认值、成员/团队深链接、角色范围和非法参数回退：

```tsx
expect(parseOrganizationRoute('')).toEqual({
  section: 'members',
  node: 'all',
  tab: 'directory',
  action: undefined,
});

expect(
  parseOrganizationRoute('?section=members&node=member:42&tab=responsibilities'),
).toMatchObject({ section: 'members', node: 'member:42', tab: 'responsibilities' });

expect(
  parseOrganizationRoute('?section=roles&scope=team&team=7&action=new-role'),
).toMatchObject({ section: 'roles', scope: 'team', teamId: 7, action: 'new-role' });

expect(
  parseOrganizationRoute('?section=broken&node=member:nope&tab=broken'),
).toEqual({ section: 'members', node: 'all', tab: 'directory', action: undefined });
```

- [ ] **Step 3: 实现路由状态模型**

`model.ts` 定义：

```tsx
export type OrganizationSection = 'members' | 'roles';
export type OrganizationNode =
  | 'organization'
  | 'all'
  | 'invites'
  | 'teams'
  | 'ungrouped'
  | `team:${number}`
  | `member:${number}`;
export type OrganizationAction = 'invite' | 'new-role' | undefined;

export type OrganizationRouteState = {
  section: OrganizationSection;
  node: OrganizationNode;
  tab: string;
  scope?: 'organization' | 'team';
  teamId?: number;
  action?: OrganizationAction;
};

export const DEFAULT_ORGANIZATION_ROUTE: OrganizationRouteState = {
  section: 'members',
  node: 'all',
  tab: 'directory',
  action: undefined,
};
```

实现 `parseOrganizationRoute(search)`、`buildOrganizationLocation(current, patch)`、`memberIdFromNode(node)`、`teamIdFromNode(node)`。只接受以下标签：

- `all`: `directory | responsibilities | access`
- `teams`: `members | roles`
- `member:*`: `profile | teams | access | responsibilities`
- `team:*`: `profile | members | roles | responsibilities`
- `invites`: `list`
- `roles`: `roles`

构建函数必须保留未被 patch 覆盖的有效状态，并返回 `/space/organization?<params>`。

- [ ] **Step 4: 注册新路由并隐藏旧入口**

在 `SPACE_PATHS` 增加：

```tsx
organization: '/space/organization',
```

空间管理路由改为：

```tsx
{
  path: SPACE_PATHS.root,
  redirect: SPACE_PATHS.organization,
},
{
  name: '组织架构',
  locale: false,
  icon: 'cluster',
  path: SPACE_PATHS.organization,
  component: './space/organization',
},
{
  path: SPACE_PATHS.members,
  hideInMenu: true,
  redirect: `${SPACE_PATHS.organization}?section=members&node=all&tab=directory`,
},
{
  path: SPACE_PATHS.invitations,
  hideInMenu: true,
  redirect: `${SPACE_PATHS.organization}?section=members&node=invites&tab=list`,
},
{
  path: SPACE_PATHS.teams,
  hideInMenu: true,
  redirect: `${SPACE_PATHS.organization}?section=members&node=teams&tab=members`,
},
{
  path: SPACE_PATHS.responsibilities,
  hideInMenu: true,
  redirect: `${SPACE_PATHS.organization}?section=members&node=all&tab=responsibilities`,
},
```

将权限旧路由保留为 `hideInMenu: true` 的固定重定向，不保留旧权限管理的嵌套菜单组：

```text
/space/access                         -> ?section=roles&scope=organization
/space/access/organization-roles      -> ?section=roles&scope=organization
/space/access/organization-bindings   -> ?section=members&node=all&tab=access
/space/access/team-roles              -> ?section=roles&scope=team
/space/access/team-bindings           -> ?section=members&node=teams&tab=roles
```

- [ ] **Step 5: 运行路由与模型测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/routes.test.ts src/pages/space/organization/model.test.ts
```

Expected: PASS。

## Task 7：建立查询键、页面壳和默认全部成员目录

**Files:**

- Create: `frontend_admin/src/pages/space/organization/queryKeys.ts`
- Create: `frontend_admin/src/pages/space/organization/styles.ts`
- Create: `frontend_admin/src/pages/space/organization/index.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/AllMembersPanel.tsx`
- Create: `frontend_admin/src/pages/space/organization/index.test.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/AllMembersPanel.test.tsx`

- [ ] **Step 1: 写页面默认行为失败测试**

测试用 QueryClient 包裹页面，mock 工作台导航和成员目录接口，断言：

```tsx
expect(screen.getByRole('tab', { name: '组织成员' })).toHaveAttribute(
  'aria-selected',
  'true',
);
expect(screen.getByText('全部成员')).toBeInTheDocument();
expect(mockListWorkspaceMembers).toHaveBeenCalledWith({
  page: 1,
  page_size: 20,
});
expect(screen.getByRole('button', { name: '邀请成员' })).toBeInTheDocument();
expect(screen.queryByRole('switch', { name: /Owner/ })).not.toBeInTheDocument();
```

`AllMembersPanel.test.tsx` 还要覆盖分页、关键字搜索回到第一页、点击成员写入 `node=member:<id>`，以及“添加已有用户”只提交 `{ user }`，不提交 `is_owner`。

- [ ] **Step 2: 定义统一查询键**

`queryKeys.ts` 使用以下层级，便于局部失效：

```tsx
export const organizationQueryKeys = {
  root: (slug?: string) => ['organization-workspace', slug] as const,
  navigation: (slug?: string) =>
    [...organizationQueryKeys.root(slug), 'navigation'] as const,
  members: (
    slug?: string,
    filters?: { page?: number; keyword?: string; teamId?: number; ungrouped?: boolean },
  ) => [...organizationQueryKeys.root(slug), 'members', filters] as const,
  member: (slug?: string, memberId?: number) =>
    [...organizationQueryKeys.root(slug), 'member', memberId] as const,
  search: (slug?: string, keyword?: string) =>
    [...organizationQueryKeys.root(slug), 'search', keyword] as const,
  invites: (slug?: string, page?: number) =>
    [...organizationQueryKeys.root(slug), 'invites', page] as const,
};
```

- [ ] **Step 3: 实现双栏样式和响应式页面壳**

`styles.ts` 使用项目现有模式，基础结构如下；其余选中、hover 和边框属性继续读取 token，不得写死颜色、圆角或阴影：

```tsx
export const useStyles = createStyles(({ css, token }) => ({
  page: css`
    display: flex;
    flex-direction: column;
    gap: ${token.marginLG}px;
  `,
  workspace: css`
    display: grid;
    grid-template-columns: minmax(260px, 320px) minmax(0, 1fr);
    gap: ${token.marginLG}px;
  `,
  treePanel: css`
    border: 1px solid ${token.colorBorderSecondary};
    border-radius: ${token.borderRadiusLG}px;
    background: ${token.colorBgContainer};
  `,
}));
```

- `page`: 垂直间距 `token.marginLG`；
- `sectionTabs`: 一级标签稳定容器；
- `workspace`: 桌面 `grid-template-columns: minmax(260px, 320px) minmax(0, 1fr)`；
- `treePanel`: `colorBgContainer`、`colorBorderSecondary`、`borderRadiusLG`；
- `contentPanel`: `min-width: 0`；
- `selectedNode`: `colorPrimaryBg`、`colorPrimaryBorder`、`colorPrimaryText`。

`index.tsx`：

- 使用 `TenantSelectionGuard title={false}`，在内容内显示实体化标题“组织架构”和说明；
- 使用 Ant Design `Tabs` 展示“组织成员 / 角色权限”；
- 解析 `useLocation().search`，通过 `history.push(buildOrganizationLocation(routeState, patch))` 更新状态；
- `Grid.useBreakpoint()` 在 `<768px` 时将左树放入共享宽度 `drawerWidthSm` 的 Drawer；
- `section=members` 的唯一主操作为“邀请成员”，写入 `node=invites&tab=list&action=invite`；
- `section=roles` 的唯一主操作为“新建角色”，写入 `action=new-role`；
- 默认右侧渲染 `AllMembersPanel`。

- [ ] **Step 4: 实现全部成员目录**

`AllMembersPanel` 使用 `ProTable<API.WorkspaceMemberOut>`，服务端分页固定 `page_size=20`，列为：成员、所属团队、房源分工状态、加入时间、操作。房源分工只显示“已配置 / 未配置”，不把 assignment 数量误称为负责房源套数。

组件 props：

```tsx
type AllMembersPanelProps = {
  initialTab: 'directory' | 'responsibilities' | 'access';
  onOpenMember: (
    memberId: number,
    tab: 'profile' | 'access' | 'responsibilities',
  ) => void;
};
```

“添加已有用户”复用 `appsOrganizationsApiSearchMembers` 和 `appsOrganizationsApiCreateMember`，提交：

```tsx
appsOrganizationsApiCreateMember({ user: values.user });
```

成功后失效 `navigation` 和全部 `members` 查询。Owner 只显示 Tag，不提供开关。

- [ ] **Step 5: 运行页面与成员目录测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/pages/space/organization/index.test.tsx src/pages/space/organization/components/AllMembersPanel.test.tsx
```

Expected: PASS。

## Task 8：实现统一组织树、团队按需加载和全局搜索

**Files:**

- Create: `frontend_admin/src/pages/space/organization/components/OrganizationTreePanel.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/OrganizationTreePanel.test.tsx`
- Modify: `frontend_admin/src/pages/space/organization/index.tsx`

- [ ] **Step 1: 写组织树失败测试**

覆盖以下用户可见行为：

```tsx
expect(screen.getByText('LAN空间')).toBeInTheDocument();
expect(screen.getByText('全部成员')).toBeInTheDocument();
expect(screen.getByText('未分组成员')).toBeInTheDocument();
expect(screen.getByText('租赁运营组')).toBeInTheDocument();

fireEvent.click(screen.getByText('租赁运营组'));
await waitFor(() =>
  expect(mockListWorkspaceMembers).toHaveBeenCalledWith({
    page: 1,
    page_size: 50,
    team_id: 7,
  }),
);

fireEvent.change(screen.getByPlaceholderText('搜索团队 / 成员'), {
  target: { value: '王' },
});
await waitFor(() => expect(mockSearchWorkspace).toHaveBeenCalledWith({ keyword: '王' }));
expect(screen.getAllByText('王小明')).toHaveLength(1);
expect(screen.getByText('租赁运营组')).toBeInTheDocument();
expect(screen.getByText('资产管理组')).toBeInTheDocument();
```

再增加一个导航返回 `pending_invite_count: null` 的用例，断言无邀请管理权限时不显示“待加入”。

- [ ] **Step 2: 实现正常树模式**

使用 Ant Design `Tree`，key 规则固定为：

```text
organization
all
invites
teams
team:<team_id>
member:<member_id>
ungrouped
load-more:<team_id>:<next_page>
```

初次只使用 navigation 中的团队摘要。展开或选择团队时请求 `listWorkspaceMembers({ page: 1, page_size: 50, team_id })`。如果 `total > items.length`，添加“加载更多”伪节点；点击后请求下一页并按 `member_id` 合并，不能重复成员。

成员可在多个团队下出现，但所有节点都调用 `onSelect('member:<member_id>')`。当前选中同时设置 Tree selected key 和可见样式，不只依赖颜色。

- [ ] **Step 3: 实现搜索模式**

输入框内容 trim 后为空时显示 Tree；非空时使用 250ms 防抖请求 `searchWorkspace({ keyword })`，用两个 `List` 分组展示“团队”和“员工”：

- 团队项显示名称和人数，点击选择 `team:<id>` 并触发团队成员加载；
- 员工项只渲染一次，显示姓名、账号和所有 `teams` 标签，点击选择 `member:<id>`；
- 无结果显示“没有找到匹配的团队或成员”，提供清空搜索动作；
- 查询失败显示 Alert 和重试按钮，不清空已加载的正常树。

- [ ] **Step 4: 接入页面壳和移动端 Drawer**

桌面始终显示树；移动端工具栏显示“组织架构”按钮，打开 Drawer。选择节点后关闭 Drawer，但关闭 Drawer 本身不触发未保存提示。

- [ ] **Step 5: 运行组织树测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/pages/space/organization/components/OrganizationTreePanel.test.tsx src/pages/space/organization/index.test.tsx
```

Expected: PASS。

## Task 9：实现团队上下文

**Files:**

- Create: `frontend_admin/src/pages/space/organization/components/TeamWorkspacePanel.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/TeamWorkspacePanel.test.tsx`
- Modify: `frontend_admin/src/pages/space/organization/index.tsx`

- [ ] **Step 1: 写团队资料和原子成员变更失败测试**

至少覆盖：

```tsx
expect(screen.getByRole('tab', { name: '团队资料' })).toBeInTheDocument();
expect(screen.getByRole('tab', { name: '团队成员' })).toBeInTheDocument();
expect(screen.getByRole('tab', { name: '团队角色' })).toBeInTheDocument();
expect(screen.getByRole('tab', { name: '分工概览' })).toBeInTheDocument();

fireEvent.click(screen.getByRole('tab', { name: '团队成员' }));
fireEvent.click(screen.getByRole('button', { name: '添加成员' }));
fireEvent.mouseDown(screen.getByLabelText('选择成员'));
fireEvent.click(screen.getByText('李思思'));
fireEvent.click(screen.getByRole('button', { name: '确认添加' }));

await waitFor(() =>
  expect(mockAddTeamMember).toHaveBeenCalledWith({ team_id: 7, user_id: 9 }),
);

fireEvent.click(screen.getByRole('button', { name: '移除 王小明' }));
fireEvent.click(screen.getByRole('button', { name: '确认移除' }));
await waitFor(() =>
  expect(mockRemoveTeamMember).toHaveBeenCalledWith({ team_id: 7, user_id: 8 }),
);
```

断言 `mockPatchTeam` 的所有调用参数都不包含 `members` 字段。

- [ ] **Step 2: 实现团队资料标签**

读取 `appsTeamsApiGetTeam({ team_id })`，用 Form 编辑 `name / phone / wechat / address / business_hours`。保存只 PATCH 这些资料字段，不携带 `members`。删除团队使用危险确认，文案完整说明：不删除组织成员、不删除个人房源分工、团队角色失效、团队资料不可恢复。

同文件导出 `TeamFormModal` 复用创建和编辑字段。无团队空状态或组织树“新建团队”动作调用 `appsTeamsApiCreateTeam({ name, phone, wechat, address, business_hours, members: [] })`；成功后刷新 navigation，并导航到新建团队的 `team:<id>`。

- [ ] **Step 3: 实现团队成员标签**

团队名册使用工作台成员接口 `team_id` 服务端分页。候选成员通过工作台成员目录关键字查询并排除已在团队中的 user ID；加入和移除分别调用新原子接口。成功后失效：

```tsx
organizationQueryKeys.navigation(slug)
organizationQueryKeys.members(slug)
organizationQueryKeys.member(slug, affectedMemberId)
['access', 'team-bindings', slug, teamId]
```

移除确认说明不会移出组织或清理个人房源分工，但会清理该团队角色。

- [ ] **Step 4: 实现团队角色绑定标签**

复用 `appsAccessApiListTeamRoles`、`appsAccessApiListTeamBindingsView`、`appsAccessApiCreateTeamBinding`、`appsAccessApiDeleteTeamBinding`。候选成员只来自当前团队分页查询；角色选项只包含 `is_active`。列表列为成员、角色、权限摘要、授权时间、移除。

- [ ] **Step 5: 实现团队分工概览入口**

调用 `appsHouseApiListStaffResponsibilities({ page, page_size: 20, team_id })`，显示成员、是否已配置、负责房源套数。页头必须标注“成员口径，成员之间的房源范围可能重叠”；点击成员调用 `onOpenMember(memberId, 'responsibilities')`。

- [ ] **Step 6: 运行团队上下文测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/pages/space/organization/components/TeamWorkspacePanel.test.tsx
```

Expected: PASS。

## Task 10：实现成员资料、所属团队和访问角色

**Files:**

- Create: `frontend_admin/src/pages/space/organization/components/MemberWorkspacePanel.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/MemberWorkspacePanel.test.tsx`
- Modify: `frontend_admin/src/pages/space/organization/index.tsx`

- [ ] **Step 1: 写成员上下文失败测试**

覆盖成员标题和四个标签：

```tsx
expect(screen.getByText('王小明')).toBeInTheDocument();
expect(screen.getByText('wangxm@example.com')).toBeInTheDocument();
expect(screen.getByRole('tab', { name: '成员资料' })).toBeInTheDocument();
expect(screen.getByRole('tab', { name: '所属团队' })).toBeInTheDocument();
expect(screen.getByRole('tab', { name: '访问角色' })).toBeInTheDocument();
expect(screen.getByRole('tab', { name: '房源分工' })).toBeInTheDocument();
expect(screen.queryByRole('switch', { name: /Owner/ })).not.toBeInTheDocument();
```

所属团队测试必须断言加入/移除调用原子接口，访问角色测试必须断言组织角色和团队角色分别调用对应 binding API。

- [ ] **Step 2: 实现成员标题和资料标签**

读取 `appsOrganizationsWorkspaceApiGetWorkspaceMember({ member_id })` 和 `appsHouseApiGetStaffResponsibility({ member_id })`。标题展示头像、姓名、账号/邮箱、Owner 只读 Tag、团队标签和“负责 N 套房源”。资料标签展示加入时间、更新时间和空间身份。

移除成员调用现有 `appsOrganizationsApiDeleteMember`，确认内容必须说明组织关系、团队关系、访问角色和房源分工都会失效或清理，且当前界面不能恢复。成功后回到 `node=all` 并失效 navigation/members 查询。

- [ ] **Step 3: 实现所属团队标签**

显示成员当前 `teams`。候选团队来自 navigation 摘要。加入/移除均以 `team.id + member.user.id` 调用原子接口；成功后同时刷新当前成员详情、相关团队成员列表、导航数量和搜索缓存。

- [ ] **Step 4: 实现访问角色标签**

组织级部分读取全部组织 bindings 后按 `binding.user.id === member.user.id` 过滤；团队级部分对成员所属团队使用 `useQueries` 加载 bindings 和 roles。提供：

- 分配组织角色：`appsAccessApiCreateOrganizationBinding({ user: userId, role: roleId })`；
- 移除组织角色：`appsAccessApiDeleteOrganizationBinding({ binding_id })`；
- 分配团队角色：`appsAccessApiCreateTeamBinding({ team_id }, { user: userId, role: roleId })`；
- 移除团队角色：`appsAccessApiDeleteTeamBinding({ team_id, binding_id })`。

按团队分组显示团队角色；成员不在某团队时不得出现该团队的角色选择器。

- [ ] **Step 5: 运行成员上下文测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/pages/space/organization/components/MemberWorkspacePanel.test.tsx
```

Expected: PASS。

## Task 11：实现顶部“角色权限”标签

**Files:**

- Create: `frontend_admin/src/pages/space/organization/components/RolePermissionPanel.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/RolePermissionPanel.test.tsx`
- Modify: `frontend_admin/src/pages/space/organization/index.tsx`
- Reuse: `frontend_admin/src/pages/access/shared.tsx`

- [ ] **Step 1: 写组织级和团队级角色定义失败测试**

组织范围测试：

```tsx
expect(screen.getByText('组织级角色')).toBeInTheDocument();
expect(mockListOrgRoles).toHaveBeenCalled();
expect(mockListTeamRoles).not.toHaveBeenCalled();

fireEvent.click(screen.getByRole('button', { name: '新建角色' }));
fireEvent.change(screen.getByLabelText('角色名称'), {
  target: { value: '招商主管' },
});
fireEvent.click(screen.getByRole('button', { name: '保存角色' }));
await waitFor(() =>
  expect(mockCreateOrgRole).toHaveBeenCalledWith(
    expect.objectContaining({ name: '招商主管' }),
  ),
);
```

团队范围测试传入 `scope="team" teamId={7}`，断言只调用 team roles/bindings 服务；系统角色不显示编辑和删除动作，自定义角色显示。再点击任一角色的“复制”，断言创建 payload 为 `{ name: '原角色名称 副本', copy_from: role.id }`。

- [ ] **Step 2: 实现统一角色表**

组件 props：

```tsx
type RolePermissionPanelProps = {
  scope: 'organization' | 'team';
  teamId?: number;
  action?: 'new-role';
  onActionHandled: () => void;
};
```

组织范围使用：

```tsx
appsAccessApiListPermissions()
appsAccessApiListOrgRoles()
appsAccessApiListOrganizationBindings()
appsAccessApiCreateOrgRole(payload)
appsAccessApiPatchOrgRole({ role_id }, payload)
appsAccessApiDeleteOrgRole({ role_id })
```

团队范围使用对应 team role/binding 服务并强制需要 `teamId`。表格列为角色、类型、权限数、授权数量、操作。复用 `RoleModal` 与 `RoleSummary`，但 Modal 的确认按钮文案改为“保存角色”。每个角色提供“复制”动作：打开创建 Modal，名称默认为“原角色名称 副本”，提交 `{ name, copy_from: sourceRole.id }`；编辑角色仍提交 `{ name, permission_keys }`。删除确认保留“已有授权引用时无法删除”的说明。

- [ ] **Step 3: 接入一级标签和组织树范围**

- 从成员区切到“角色权限”时，如果当前节点是 `team:<id>`，写入 `scope=team&team=<id>`；否则使用 `scope=organization`。
- 角色区点击组织节点切换为组织范围；点击团队节点切换为团队范围。
- 角色区点击成员节点时切回 `section=members&node=member:<id>&tab=access`。
- `scope=team` 但 URL 没有团队 ID 时，选择 navigation 中第一个可见团队并用 `history.replace` 补齐 `team=<id>`；没有团队时显示可创建团队的空状态。
- URL 中 `action=new-role` 时自动打开当前范围的创建角色 Modal，关闭或保存后清除 action。

- [ ] **Step 4: 运行角色权限测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/pages/space/organization/components/RolePermissionPanel.test.tsx src/pages/space/organization/index.test.tsx
```

Expected: PASS。

## Task 12：提取并整合房源分工编辑器

**Files:**

- Create: `frontend_admin/src/pages/space/organization/components/ResponsibilityEditor.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/ResponsibilityEditor.test.tsx`
- Modify: `frontend_admin/src/pages/space/organization/components/MemberWorkspacePanel.tsx`
- Reference: `frontend_admin/src/pages/rental/responsibilities/index.tsx`
- Reference: `frontend_admin/src/pages/rental/responsibilities/index.test.tsx`

- [ ] **Step 1: 将原分工页核心测试移植为组件测试**

测试 props 固定为：

```tsx
<ResponsibilityEditor
  memberId={12}
  memberName="王小明"
  onDirtyStateChange={mockDirtyChange}
/>
```

覆盖：

- 初次调用 `appsHouseApiGetStaffResponsibility({ member_id: 12 })`；
- 房东、楼栋、小区分别显示已选值和数量；
- 保存前先打开“确认替换分工”；
- 三层为空时出现“取消全部负责范围”错误提示；
- 确认后调用 `appsHouseApiReplaceStaffResponsibilities({ member_id: 12 }, payload)`；
- “查看当前负责房源”仅查询已保存结果，不把草稿称为预览；
- 修改表单后调用 `onDirtyStateChange({ dirty: true, save, reset })`。

- [ ] **Step 2: 提取现有安全编辑逻辑**

从原页面迁移并保留以下函数/组件，不改变业务语义：

```tsx
const EMPTY_RESPONSIBILITIES = {
  landlord_ids: [],
  building_ids: [],
  estate_ids: [],
};

function normalizeResponsibilities(values: API.PropertyResponsibilityUpdateIn) {
  return {
    landlord_ids: values.landlord_ids || [],
    building_ids: values.building_ids || [],
    estate_ids: values.estate_ids || [],
  };
}
```

保留 `ResponsibilityScopeSection`、`selectedOptionLabels`、三级优先级说明、整体替换确认和负责房源 Drawer。颜色必须改用 token 或 Ant Design 语义组件，不新增硬编码十六进制值。

- [ ] **Step 3: 接入成员“房源分工”标签**

成员选中时才加载编辑器。保存成功后失效：

```tsx
['house', 'staff-responsibilities']
['house', 'responsible-houses']
organizationQueryKeys.member(slug, memberId)
```

成员标题中的负责房源数量随保存结果同步更新。

- [ ] **Step 4: 保持旧分工页源码不动**

旧文件继续保留作为兼容源码，不从新页面 import 整个页面组件，也不删除其测试。共享逻辑只在新组件中形成清晰边界，后续清理由独立任务处理。

- [ ] **Step 5: 运行分工组件与后端回归测试**

Run:

```bash
docker compose exec web pytest tests/house/test_responsibilities.py -q
cd frontend_admin
nvm use 22
npm exec -- vitest run src/pages/space/organization/components/ResponsibilityEditor.test.tsx src/pages/rental/responsibilities/index.test.tsx
```

Expected: PASS。

## Task 13：整合邀请管理和邀请状态语义

**Files:**

- Modify: `frontend_admin/src/components/AppIcon/index.tsx`
- Modify: `frontend_admin/src/components/AppIcon/index.test.tsx`
- Modify: `frontend_admin/src/components/AppStatus/registry.ts`
- Modify: `frontend_admin/src/components/AppStatus/index.test.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/InvitationWorkspacePanel.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/InvitationWorkspacePanel.test.tsx`
- Modify: `frontend_admin/src/pages/space/organization/index.tsx`
- Reference: `frontend_admin/src/pages/space/invites/index.tsx`

- [ ] **Step 1: 先写邀请状态注册测试**

新增断言：

```tsx
expect(resolveAppIconDefinition('organization-invite.pending')).toMatchObject({
  matched: true,
});
expect(
  resolveAppStatusDefinition('organization-invite', 'pending'),
).toMatchObject({ tone: 'info', matched: true });
expect(
  resolveAppStatusDefinition('organization-invite', 'stale'),
).toMatchObject({ tone: 'warning', matched: true });
expect(
  resolveAppStatusDefinition('organization-invite', 'expired'),
).toMatchObject({ tone: 'error', matched: true });
```

- [ ] **Step 2: 注册邀请图标和状态**

在 `APP_ICON_DEFINITIONS` 增加静态名称：

```tsx
'organization-invite': { icon: 'solar:letter-outline' },
'organization-invite.pending': { icon: 'solar:clock-circle-outline' },
'organization-invite.stale': { icon: 'solar:danger-triangle-outline' },
'organization-invite.expired': { icon: 'solar:close-circle-outline' },
```

在 `APP_STATUS_DEFINITIONS` 增加：

```tsx
'organization-invite': {
  states: {
    pending: { icon: 'organization-invite.pending', tone: 'info' },
    stale: { icon: 'organization-invite.stale', tone: 'warning' },
    expired: { icon: 'organization-invite.expired', tone: 'error' },
  },
},
```

- [ ] **Step 3: 写邀请工作台失败测试**

覆盖：

```tsx
expect(mockListInvites).toHaveBeenCalledWith({ page: 1, page_size: 20 });
expect(screen.getByText('待加入')).toBeInTheDocument();
expect(screen.getByText('已过期')).toBeInTheDocument();

fireEvent.click(screen.getByRole('button', { name: '邀请成员' }));
expect(screen.getByRole('dialog', { name: '邀请成员' })).toBeInTheDocument();
expect(screen.queryByLabelText('预设团队')).not.toBeInTheDocument();

fireEvent.click(screen.getByText('重发'));
await waitFor(() => expect(mockResendInvite).toHaveBeenCalled());
expect(mockInvalidateQueries).toHaveBeenCalledWith({
  queryKey: organizationQueryKeys.navigation('acme'),
});
```

用 `window.dispatchEvent(new Event('focus'))` 验证页面重新获得焦点时触发邀请查询 refetch。

- [ ] **Step 4: 实现邀请列表和动作**

移植现有邮箱、手机号、站内用户邀请方式和 `buildInvitePayload()`，仍只允许预设组织级访问角色。列表改为服务端分页 `page_size=20`。状态函数固定为：

```tsx
export function inviteState(invite: API.InviteOut) {
  if (invite.is_expired) return 'expired';
  if (dayjs().diff(dayjs(invite.created_at), 'day') >= 3) return 'stale';
  return 'pending';
}
```

使用：

```tsx
<AppStatusTag name="organization-invite" state={state}>
  {state === 'expired' ? '已过期' : state === 'stale' ? '长时间未处理' : '待加入'}
</AppStatusTag>
```

详情、重发、取消继续复用现有接口。创建、重发、取消成功后同时失效邀请列表和 navigation；邀请 Query 显式设置 `refetchOnWindowFocus: true`。

- [ ] **Step 5: 接入 URL 动作**

`node=invites&action=invite` 时自动打开邀请 Modal。关闭或创建成功后清除 `action`，但保持 `node=invites`。从团队节点发起也不携带团队字段。

- [ ] **Step 6: 运行邀请与状态测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/components/AppIcon/index.test.tsx src/components/AppStatus/index.test.tsx src/pages/space/organization/components/InvitationWorkspacePanel.test.tsx src/pages/space/invites/index.test.tsx
```

Expected: PASS。

## Task 14：实现未保存保护、组织概览和完整页面状态

**Files:**

- Create: `frontend_admin/src/pages/space/organization/hooks/useUnsavedWorkspaceGuard.ts`
- Create: `frontend_admin/src/pages/space/organization/hooks/useUnsavedWorkspaceGuard.test.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/OrganizationOverviewPanel.tsx`
- Create: `frontend_admin/src/pages/space/organization/components/OrganizationOverviewPanel.test.tsx`
- Modify: `frontend_admin/src/pages/space/organization/index.tsx`
- Modify: `frontend_admin/src/pages/space/organization/components/MemberWorkspacePanel.tsx`
- Modify: `frontend_admin/src/pages/space/organization/components/TeamWorkspacePanel.tsx`
- Modify: `frontend_admin/src/pages/space/organization/components/RolePermissionPanel.tsx`
- Modify: `frontend_admin/src/pages/space/organization/components/ResponsibilityEditor.tsx`

- [ ] **Step 1: 写未保存保护失败测试**

hook 接口固定为：

```tsx
type UnsavedWorkspaceRegistration = {
  dirty: boolean;
  save?: () => Promise<void>;
  reset: () => void;
};

const guard = useUnsavedWorkspaceGuard(registration);
await guard.requestTransition(() => onSelectNode('team:7'));
```

测试以下行为：

- `dirty=false` 时立即执行 transition；
- `dirty=true` 时显示“存在未保存修改”；
- “继续编辑”不执行 transition；
- “放弃修改”调用 `reset()` 后执行 transition；
- 有 `save` 时显示“保存并继续”，保存成功后执行 transition，保存失败时保留当前页面；
- active 时注册 `beforeunload` 和 `history.block`，unmount 时解除。

- [ ] **Step 2: 实现统一 guard**

参考现有 `useUnsavedWorkbenchGuard` 的 `history.block` 模式，但通过自定义 Modal footer 提供三种动作：

```tsx
<Button onClick={continueEditing}>继续编辑</Button>
<Button danger onClick={discardAndContinue}>放弃修改</Button>
{registration.save ? (
  <Button type="primary" loading={saving} onClick={saveAndContinue}>
    保存并继续
  </Button>
) : null}
```

树节点、一级标签、详情标签和路由离开统一走 `requestTransition()`；关闭移动端组织树 Drawer 不调用 guard。

- [ ] **Step 3: 实现组织概览和团队目录空状态**

`OrganizationOverviewPanel` 使用 navigation 展示成员、Owner、团队、未分组、待加入和未配置房源分工数量。只使用可点击摘要卡，不增加图表。`pending_invite_count === null` 时隐藏邀请摘要。

当旧 `/space/teams` 重定向产生 `node=teams`：

- 有团队时自动 `history.replace` 到第一个可见 `team:<id>`；原 `tab=roles` 保留为团队角色，其余情况落到团队成员；
- 无团队时显示创建团队空状态和“新建团队”动作；
- 不在 URL 中留下不存在的团队 ID。

- [ ] **Step 4: 补齐加载、失败、无权限和删除后回退**

页面必须满足：

- navigation 初次加载显示结构化 Skeleton；
- 左树失败显示 Alert、原因和“重新加载”；
- 右侧请求失败只替换当前面板，不清空树；
- 分页无结果和筛选无结果使用不同文案；
- API 403 显示“当前角色没有执行此操作的权限”，保留可查看内容；
- 团队或成员被其他管理员删除并返回 404 时，回退 `all` 并提示；
- 保存失败保留表单值；
- 明暗主题下 selected、hover、border 均来自 token。

- [ ] **Step 5: 运行 guard、概览和页面集成测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/pages/space/organization/hooks/useUnsavedWorkspaceGuard.test.tsx src/pages/space/organization/components/OrganizationOverviewPanel.test.tsx src/pages/space/organization/index.test.tsx
```

Expected: PASS。

## Task 15：全量验证和浏览器视觉验收

**Files:**

- Verify only; screenshots under `.codex/audits/unified-organization-workspace/`

- [ ] **Step 1: 运行后端定向测试**

Run:

```bash
docker compose exec web pytest tests/organizations/test_workspace_api.py tests/organizations/test_api.py tests/teams/test_api.py tests/access/test_api.py tests/house/test_responsibilities.py -q
```

Expected: PASS。

- [ ] **Step 2: 运行管理端定向测试**

Run:

```bash
cd frontend_admin
nvm use 22
npm exec -- vitest run src/routes.test.ts src/pages/space/organization src/components/AppIcon/index.test.tsx src/components/AppStatus/index.test.tsx
```

Expected: PASS。

- [ ] **Step 3: 运行类型、规范和构建检查**

Run:

```bash
cd frontend_admin
nvm use 22
npm run lint
npm exec -- antd lint ./src/pages/space/organization
npm run build
```

Expected: 全部 exit code 0；不修改 `package.json`、`package-lock.json` 或多语言文件。

- [ ] **Step 4: 浏览器桌面验收**

在 `http://localhost:8080/dashboard/space/organization` 验收并截图：

1. 默认“组织成员 → 全部成员”；
2. 展开团队后按需出现成员；
3. 搜索团队和多团队成员，成员只出现一次且有团队标签；
4. 成员四个标签完整，Owner 不可行内修改；
5. 团队成员加入/移除使用单成员动作；
6. 组织级和团队级角色定义可切换；
7. 邀请创建、重发、取消和过期状态；
8. 房源分工三级优先级、整体替换确认和清空危险提示；
9. 旧五类路由进入正确上下文。

截图写入：

```text
.codex/audits/unified-organization-workspace/desktop-all-members.png
.codex/audits/unified-organization-workspace/desktop-member-detail.png
.codex/audits/unified-organization-workspace/desktop-team-detail.png
.codex/audits/unified-organization-workspace/desktop-role-permissions.png
.codex/audits/unified-organization-workspace/desktop-invites.png
```

- [ ] **Step 5: 浏览器窄屏与键盘验收**

在 390×844 视口确认：组织树进入 Drawer、标签可横向滚动、主要动作可见、表单单列、确认弹窗可完成。使用 Tab / Shift+Tab / Enter / Space 验证树、标签、表单、Modal 和 Drawer 可操作，并确认状态不仅依赖颜色。

截图写入：

```text
.codex/audits/unified-organization-workspace/mobile-tree-drawer.png
.codex/audits/unified-organization-workspace/mobile-member-responsibility.png
```

- [ ] **Step 6: 最终范围检查**

Run:

```bash
rg -n "NotImplementedError|^[[:space:]]*pass([[:space:]]|$)" apps/organizations/workspace_api.py apps/organizations/workspace_schemas.py apps/teams/api.py frontend_admin/src/pages/space/organization
```

Expected: 无输出。确认原页面文件仍存在，生成截图不在项目根目录，没有依赖或多语言文件改动。

## 实施完成标准

- `/space/organization` 成为唯一可见的组织治理菜单入口。
- 默认展示全部组织成员，左侧统一组织树在桌面和移动端均可用。
- 团队成员不再通过完整集合 PATCH 进行日常增删，移出团队时清理团队角色绑定。
- 角色定义与角色分配边界清晰：顶部“角色权限”定义角色，成员/团队上下文分配角色。
- 邀请、团队、成员、角色、房源分工及旧路由兼容全部通过测试和浏览器验收。
- 不新增数据库迁移，不物理删除旧页面，不修改依赖和多语言文件。
