---
status: accepted
---

# 将收藏定义为普通用户通用能力

收藏归属于普通用户，而不是租户或某个具体业务域。平台使用统一收藏核心维护用户与公开业务对象之间的关系，由房源、文章、商品等目标适配器分别负责可见性校验和安全摘要；`HouseFavorite` 仅作为历史迁移兼容模型保留，新写入统一进入 `Favorite`。相比每个业务独立建表，此选择能统一幂等、软取消和目标下架后的行为；相比直接暴露 Django `ContentType`，显式目标类型注册表可以保持公共协议稳定，并强制每种业务定义自己的公开数据边界。
