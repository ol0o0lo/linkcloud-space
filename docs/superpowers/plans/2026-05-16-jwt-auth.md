# JWT 认证适配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在保持 Web SPA 的 Session 认证不变的前提下，为 Ninja 和 DRF 接口同时接入 JWT 认证，使小程序等无 cookie 端可以通过 `Authorization: Bearer <token>` + `X-Org-Slug` header 访问所有现有接口。

**Architecture:** 使用 `djangorestframework-simplejwt` 提供 JWT 颁发和验证能力。扩展 `OrganizationMiddleware` 在 session 无 org 时 fallback 到 `X-Org-Slug` header。Ninja 的全局 auth 替换为自定义 `HttpBearer` 子类，优先验证 JWT，fallback 到 session；DRF 配置双认证类（JWT + Session）。旧接口代码零改动。

**Tech Stack:** djangorestframework, djangorestframework-simplejwt, django-ninja（已有）

---

## 文件结构

| 文件 | 操作 | 说明 |
|------|------|------|
| `pyproject.toml` | 修改 | 添加 djangorestframework、djangorestframework-simplejwt 依赖 |
| `config/settings/_base.py` | 修改 | INSTALLED_APPS、REST_FRAMEWORK、SIMPLE_JWT 配置 |
| `config/ninja_auth.py` | 新建 | Ninja 的 JWT+Session 双认证类 |
| `config/api.py` | 修改 | 替换 `auth=django_auth` 为自定义认证类 |
| `config/drf_exceptions.py` | 新建 | DRF exception handler，统一错误格式与 Ninja 一致 |
| `config/urls.py` | 修改 | 注册 JWT token/refresh 端点 |
| `apps/organizations/session.py` | 修改 | 新增 `get_organization_from_header()` |
| `apps/organizations/middleware.py` | 修改 | fallback 到 header 读 org |
| `apps/accounts/tests/test_jwt_auth.py` | 新建 | JWT 认证集成测试 |
| `apps/organizations/tests/test_jwt_org_header.py` | 新建 | X-Org-Slug header 测试 |

---

### Task 1: 安装依赖并配置 settings

**Files:**
- Modify: `pyproject.toml`
- Modify: `config/settings/_base.py`

- [ ] **Step 1: 添加依赖**

编辑 `pyproject.toml`，在 `dependencies` 列表中添加（紧跟其他 django 相关依赖之后）：

```toml
"djangorestframework>=3.16,<4",
"djangorestframework-simplejwt>=5.4,<6",
```

- [ ] **Step 2: 安装依赖**

```bash
uv sync
```

预期：无报错，`rest_framework` 和 `rest_framework_simplejwt` 可 import。

- [ ] **Step 3: 配置 settings**

在 `config/settings/_base.py` 的 `INSTALLED_APPS` 列表中，在 `"hijack"` 之后添加：

```python
    "rest_framework",
    "rest_framework_simplejwt",
    "rest_framework_simplejwt.token_blacklist",
```

在 `config/settings/_base.py` 末尾（现有配置之后）添加：

```python
# ---------------------------------------------------------------------------
# Django REST Framework
# ---------------------------------------------------------------------------
REST_FRAMEWORK = {
    "DEFAULT_AUTHENTICATION_CLASSES": [
        "rest_framework_simplejwt.authentication.JWTAuthentication",
        "rest_framework.authentication.SessionAuthentication",
    ],
    "DEFAULT_PERMISSION_CLASSES": [
        "rest_framework.permissions.IsAuthenticated",
    ],
    "EXCEPTION_HANDLER": "config.drf_exceptions.custom_exception_handler",
}

# ---------------------------------------------------------------------------
# SimpleJWT
# ---------------------------------------------------------------------------
from datetime import timedelta  # noqa: E402

SIMPLE_JWT = {
    "ACCESS_TOKEN_LIFETIME": timedelta(minutes=30),
    "REFRESH_TOKEN_LIFETIME": timedelta(days=7),
    "ROTATE_REFRESH_TOKENS": True,
    "BLACKLIST_AFTER_ROTATION": True,
    "UPDATE_LAST_LOGIN": True,
    "AUTH_HEADER_TYPES": ("Bearer",),
    "USER_ID_FIELD": "id",
    "USER_ID_CLAIM": "user_id",
}
```

- [ ] **Step 4: 创建 token_blacklist migration**

```bash
docker compose exec web python manage.py migrate
```

预期：`rest_framework_simplejwt.token_blacklist` 表创建成功，无报错。

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml config/settings/_base.py
git commit -m "feat: 添加 djangorestframework 和 simplejwt 依赖及配置"
```

---

### Task 2: DRF 统一异常格式

**Files:**
- Create: `config/drf_exceptions.py`

**背景：** `apps/base/permissions.py` 里的 `require_*` 函数 raise 的是 `django.core.exceptions.PermissionDenied`，不是 DRF 的异常，DRF 默认不处理它。需要自定义 exception handler 统一格式，与 Ninja 返回的 `{"detail": "..."}` 保持一致。

- [ ] **Step 1: 创建 `config/drf_exceptions.py`**

```python
"""
DRF 全局异常处理器。

统一错误响应格式为 {"detail": "..."} 或 {"field": ["..."]}，
与 Ninja 侧（apps/base/errors.py）保持一致，方便前端/小程序统一处理。
"""

from django.core.exceptions import PermissionDenied as DjangoPermissionDenied
from django.core.exceptions import ValidationError as DjangoValidationError
from django.http import Http404

from rest_framework import status
from rest_framework.response import Response
from rest_framework.views import exception_handler


def custom_exception_handler(exc, context):
    # 先走 DRF 默认处理（处理 DRF 自己的 APIException 子类）
    response = exception_handler(exc, context)
    if response is not None:
        return response

    # Django 原生 PermissionDenied → 403
    if isinstance(exc, DjangoPermissionDenied):
        return Response(
            {"detail": str(exc) or "Permission denied."},
            status=status.HTTP_403_FORBIDDEN,
        )

    # Django 原生 Http404 → 404
    if isinstance(exc, Http404):
        return Response({"detail": "Not found."}, status=status.HTTP_404_NOT_FOUND)

    # Django ValidationError → 400
    if isinstance(exc, DjangoValidationError):
        if hasattr(exc, "message_dict"):
            data = dict(exc.message_dict)
            if "__all__" in data:
                data["non_field_errors"] = data.pop("__all__")
        elif hasattr(exc, "messages"):
            data = {"non_field_errors": list(exc.messages)}
        else:
            data = {"non_field_errors": [str(exc)]}
        return Response(data, status=status.HTTP_400_BAD_REQUEST)

    # 其他未处理异常不返回 response，让 Django 默认 500 处理
    return None
```

- [ ] **Step 2: Commit**

```bash
git add config/drf_exceptions.py
git commit -m "feat: 添加 DRF 自定义异常处理器，统一错误格式"
```

---

### Task 3: Ninja 双认证适配

**Files:**
- Create: `config/ninja_auth.py`
- Modify: `config/api.py`

**背景：** 当前 `config/api.py` 使用 `auth=django_auth`（纯 Session）。需要替换为自定义认证类：优先验证 JWT Bearer token，fallback 到 session，保证 Web 端完全不受影响。

- [ ] **Step 1: 创建 `config/ninja_auth.py`**

```python
"""
Ninja 双认证：JWT Bearer 优先，fallback 到 Django session。

Web SPA 继续用 session（X-CSRFToken），无感知。
小程序 / 移动端使用 Authorization: Bearer <access_token>。
"""

from ninja.security import HttpBearer
from rest_framework_simplejwt.authentication import JWTAuthentication
from rest_framework_simplejwt.exceptions import InvalidToken, TokenError


class JWTOrSessionAuth(HttpBearer):
    """
    Ninja 全局认证类。

    1. 若请求头有 Authorization: Bearer <token>，走 simplejwt 验证。
    2. 否则 fallback 到 Django session（与原 django_auth 行为一致）。
    """

    openapi_scheme = "bearer"

    def authenticate(self, request, token: str):
        # --- JWT 路径 ---
        if token:
            jwt_auth = JWTAuthentication()
            try:
                validated_token = jwt_auth.get_validated_token(token.encode())
                user = jwt_auth.get_user(validated_token)
                request.user = user
                return user
            except (InvalidToken, TokenError):
                return None

        return None

    def __call__(self, request):
        # Bearer token 存在时走 JWT
        auth_header = request.META.get("HTTP_AUTHORIZATION", "")
        if auth_header.startswith("Bearer "):
            token = auth_header[7:]
            return self.authenticate(request, token)

        # 无 Bearer header → fallback 到 session
        if request.user and request.user.is_authenticated:
            return request.user

        return None
```

- [ ] **Step 2: 修改 `config/api.py`**

将文件顶部的 import：
```python
from ninja.security import django_auth
```
替换为：
```python
from config.ninja_auth import JWTOrSessionAuth
```

将 `NinjaAPI` 实例化中的：
```python
    auth=django_auth,
```
替换为：
```python
    auth=JWTOrSessionAuth(),
```

- [ ] **Step 3: Commit**

```bash
git add config/ninja_auth.py config/api.py
git commit -m "feat: Ninja 认证替换为 JWT+Session 双认证"
```

---

### Task 4: JWT token 端点注册

**Files:**
- Modify: `config/urls.py`

- [ ] **Step 1: 修改 `config/urls.py`**

在文件顶部 import 区域添加：
```python
from rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView
```

在 `urlpatterns` 列表中，`path("api/", ninja_api.urls)` 之前添加：
```python
    path("api/v1/auth/token/", TokenObtainPairView.as_view(), name="token_obtain_pair"),
    path("api/v1/auth/token/refresh/", TokenRefreshView.as_view(), name="token_refresh"),
```

完整的 urlpatterns 开头应如下：
```python
urlpatterns: list[URLResolver | URLPattern] = [
    path("_allauth/", include("allauth.headless.urls")),
    path("api/v1/auth/token/", TokenObtainPairView.as_view(), name="token_obtain_pair"),
    path("api/v1/auth/token/refresh/", TokenRefreshView.as_view(), name="token_refresh"),
    path("api/", ninja_api.urls),
    ...
]
```

- [ ] **Step 2: Commit**

```bash
git add config/urls.py
git commit -m "feat: 注册 JWT token 获取和刷新端点"
```

---

### Task 5: OrganizationMiddleware 支持 X-Org-Slug Header

**Files:**
- Modify: `apps/organizations/session.py`
- Modify: `apps/organizations/middleware.py`

**背景：** Web 端通过 `POST /api/organizations/{slug}/select/` 把 org 写入 session，后续所有请求从 session 读 `request.org`。小程序没有 session，改为从请求头 `X-Org-Slug` 读取，并验证当前 user 是否是该 org 成员。

- [ ] **Step 1: 在 `apps/organizations/session.py` 末尾添加**

```python
def get_organization_from_header(request) -> MockedOrg:
    """
    从 X-Org-Slug 请求头解析 org，用于无 session 的端（小程序、移动端等）。

    要求 request.user 已经通过 JWT 认证（AuthenticationMiddleware 之后调用）。
    如果 slug 无效或 user 不是成员，返回空 MockedOrg（后续 require_org_* 会报 403）。
    """
    slug = request.headers.get("X-Org-Slug", "").strip()
    if not slug:
        return MockedOrg()

    if not (hasattr(request, "user") and request.user.is_authenticated):
        return MockedOrg()

    org_model = apps.get_model("organizations.Organization")
    org = org_model.objects.filter(slug=slug).first()
    if org is None:
        return MockedOrg()

    # 验证成员身份（防止越权访问其他 org）
    from apps.organizations.models import OrganizationMember

    membership = OrganizationMember.objects.filter(organization=org, user=request.user).first()
    if membership is None:
        return MockedOrg()

    return MockedOrg(
        pk=org.pk,
        id=org.pk,
        name=org.name,
        slug=org.slug,
        is_owner=membership.is_owner,
    )
```

- [ ] **Step 2: 修改 `apps/organizations/middleware.py`**

将整个文件替换为：

```python
from django.utils.deprecation import MiddlewareMixin
from django.utils.functional import SimpleLazyObject

from apps.organizations.session import get_organization, get_organization_from_header


def _get_organization(request):
    request._cached_organization = getattr(request, "_cached_organization", None)
    if not request._cached_organization:
        # 1. 优先从 session 读（Web SPA，保持原有行为）
        org = get_organization(request)

        # 2. session 无 org 且有 X-Org-Slug header → 小程序/移动端路径
        if not org.pk:
            org = get_organization_from_header(request)

        request._cached_organization = org
    return request._cached_organization


class OrganizationMiddleware(MiddlewareMixin):
    @staticmethod
    def process_request(request):
        assert hasattr(request, "session"), (  # noqa: S101
            "The Django authentication middleware requires session middleware "
            "to be installed. Edit your MIDDLEWARE setting to insert "
            "'django.contrib.sessions.middleware.SessionMiddleware' before "
            "'django.contrib.auth.middleware.AuthenticationMiddleware'."
        )
        request.org = SimpleLazyObject(lambda: _get_organization(request))
```

- [ ] **Step 3: Commit**

```bash
git add apps/organizations/session.py apps/organizations/middleware.py
git commit -m "feat: OrganizationMiddleware 支持 X-Org-Slug header fallback"
```

---

### Task 6: 编写集成测试

**Files:**
- Create: `apps/accounts/tests/test_jwt_auth.py`
- Create: `apps/organizations/tests/test_jwt_org_header.py`

- [ ] **Step 1: 创建 `apps/accounts/tests/test_jwt_auth.py`**

```python
"""
JWT 认证集成测试。

验证：
- 正确凭据可获取 access + refresh token
- access token 可访问受保护的 Ninja 接口
- 无效 token 返回 401
- session 认证仍然有效（Web 端回归）
"""

import pytest
from django.test import Client


@pytest.fixture
def user(db):
    from model_bakery import baker
    return baker.make("accounts.User", is_active=True, username="testjwt", email="jwt@example.com")


@pytest.fixture
def user_with_password(db):
    from django.contrib.auth import get_user_model
    User = get_user_model()
    user = User.objects.create_user(username="jwtuser", email="jwtuser@example.com", password="testpass123")
    return user


def test_obtain_token_success(client, user_with_password):
    """正确凭据可获取 token。"""
    resp = client.post(
        "/api/v1/auth/token/",
        {"username": "jwtuser", "password": "testpass123"},
        content_type="application/json",
    )
    assert resp.status_code == 200
    data = resp.json()
    assert "access" in data
    assert "refresh" in data


def test_obtain_token_wrong_password(client, user_with_password):
    """错误密码返回 401。"""
    resp = client.post(
        "/api/v1/auth/token/",
        {"username": "jwtuser", "password": "wrong"},
        content_type="application/json",
    )
    assert resp.status_code == 401


def test_access_ninja_endpoint_with_jwt(client, user_with_password):
    """用 JWT access token 可访问 Ninja 保护接口。"""
    # 获取 token
    resp = client.post(
        "/api/v1/auth/token/",
        {"username": "jwtuser", "password": "testpass123"},
        content_type="application/json",
    )
    access = resp.json()["access"]

    # 访问受保护接口（/api/version/ 是 auth=None，用 /api/users/ 需认证）
    resp = client.get(
        "/api/users/",
        HTTP_AUTHORIZATION=f"Bearer {access}",
    )
    # 能通过认证（即使返回空列表也是 200，不是 401/403）
    assert resp.status_code in (200, 403)  # 403 是权限不足，但认证通过了


def test_invalid_jwt_returns_401(client):
    """无效 JWT 返回 401。"""
    resp = client.get(
        "/api/users/",
        HTTP_AUTHORIZATION="Bearer invalidtoken",
    )
    assert resp.status_code == 401


def test_no_auth_returns_401(client):
    """无认证信息返回 401。"""
    resp = client.get("/api/users/")
    assert resp.status_code == 401


def test_refresh_token(client, user_with_password):
    """refresh token 可换取新 access token。"""
    resp = client.post(
        "/api/v1/auth/token/",
        {"username": "jwtuser", "password": "testpass123"},
        content_type="application/json",
    )
    refresh = resp.json()["refresh"]

    resp = client.post(
        "/api/v1/auth/token/refresh/",
        {"refresh": refresh},
        content_type="application/json",
    )
    assert resp.status_code == 200
    assert "access" in resp.json()


def test_session_auth_still_works(client, user_with_password):
    """Web 端 session 认证不受影响（回归测试）。"""
    client.force_login(user_with_password)
    resp = client.get("/api/version/")
    assert resp.status_code == 200
```

- [ ] **Step 2: 运行测试，预期全部 FAIL（接口未实现时）或 PASS（已实现后验证）**

```bash
docker compose exec web pytest apps/accounts/tests/test_jwt_auth.py -v
```

- [ ] **Step 3: 创建 `apps/organizations/tests/test_jwt_org_header.py`**

```python
"""
X-Org-Slug header 测试。

验证小程序通过 JWT + X-Org-Slug header 可正确解析 request.org。
"""

import pytest


@pytest.fixture
def user_with_password(db):
    from django.contrib.auth import get_user_model
    User = get_user_model()
    return User.objects.create_user(username="orgtest", email="orgtest@example.com", password="testpass123")


@pytest.fixture
def org_with_member(db, user_with_password):
    from model_bakery import baker
    from apps.organizations.models import OrganizationMember
    org = baker.make("organizations.Organization", name="Test Org", slug="test-org")
    OrganizationMember.objects.create(organization=org, user=user_with_password, is_owner=True)
    return org


def _get_access_token(client, username="orgtest", password="testpass123"):
    resp = client.post(
        "/api/v1/auth/token/",
        {"username": username, "password": password},
        content_type="application/json",
    )
    return resp.json()["access"]


def test_org_resolved_from_header(client, org_with_member, user_with_password):
    """JWT + X-Org-Slug header 可正确访问需要 org 的接口。"""
    token = _get_access_token(client)

    resp = client.get(
        "/api/teams/",
        HTTP_AUTHORIZATION=f"Bearer {token}",
        HTTP_X_ORG_SLUG="test-org",
    )
    assert resp.status_code == 200


def test_wrong_org_slug_returns_403(client, org_with_member, user_with_password):
    """不存在的 org slug 应返回 403。"""
    token = _get_access_token(client)

    resp = client.get(
        "/api/teams/",
        HTTP_AUTHORIZATION=f"Bearer {token}",
        HTTP_X_ORG_SLUG="nonexistent-org",
    )
    assert resp.status_code == 403


def test_not_member_org_returns_403(client, db, user_with_password):
    """不是成员的 org 应返回 403。"""
    from model_bakery import baker
    other_org = baker.make("organizations.Organization", slug="other-org")

    token = _get_access_token(client)

    resp = client.get(
        "/api/teams/",
        HTTP_AUTHORIZATION=f"Bearer {token}",
        HTTP_X_ORG_SLUG="other-org",
    )
    assert resp.status_code == 403


def test_no_org_header_returns_403(client, org_with_member, user_with_password):
    """JWT 认证通过但无 X-Org-Slug header，需要 org 的接口返回 403。"""
    token = _get_access_token(client)

    resp = client.get(
        "/api/teams/",
        HTTP_AUTHORIZATION=f"Bearer {token}",
    )
    assert resp.status_code == 403
```

- [ ] **Step 4: 运行所有新测试**

```bash
docker compose exec web pytest apps/accounts/tests/test_jwt_auth.py apps/organizations/tests/test_jwt_org_header.py -v
```

预期：全部 PASS。

- [ ] **Step 5: 运行全量测试，确保无回归**

```bash
docker compose exec web pytest --ignore=e2e -x -q
```

预期：原有 8 个已知失败不变，无新增失败。

- [ ] **Step 6: Commit**

```bash
git add apps/accounts/tests/test_jwt_auth.py apps/organizations/tests/test_jwt_org_header.py
git commit -m "test: 添加 JWT 认证和 X-Org-Slug header 集成测试"
```

---

## 自检

**Spec coverage：**
- ✅ Ninja 接口支持 JWT → Task 3
- ✅ DRF 接口支持 JWT → Task 1（REST_FRAMEWORK 配置）
- ✅ Web 端 session 不受影响 → Task 3（fallback）+ Task 6（回归测试）
- ✅ X-Org-Slug header 解析 → Task 5
- ✅ 成员身份验证（防越权）→ Task 5
- ✅ 错误格式统一 → Task 2
- ✅ token 获取/刷新端点 → Task 4

**Placeholder scan：** 无 TBD/TODO。

**Type consistency：** `MockedOrg` 在 Task 5 中与 `session.py` 现有定义完全一致。
